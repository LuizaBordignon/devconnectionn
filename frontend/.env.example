# URL base da API. Em produção, aponte para o backend no Railway
# e configure isso como variável de ambiente no projeto da Vercel
# (Settings > Environment Variables), não neste arquivo.
VITE_API_URL=http://localhost:8000/api
